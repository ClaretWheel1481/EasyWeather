name: easyweather
description: easyweather by claret

publish_to: 'none' 

version: 2.0.1+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences:
  http: 
  dynamic_color:
  geolocator: 
  geocoding: 
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

flutter:
  generate: true
  uses-material-design: true